# Лабораторная работа №3. Управляющие конструкции
## Условие
### Условные конструкции
1. Используя функцию date(), создайте таблицу с расписанием, формируемым на основе текущего дня недели.  
<img width="1155" height="162" alt="изображение" src="https://github.com/user-attachments/assets/333d009a-0e78-4cd7-b019-a9568a42a785" />

- Для John Styles (xx - xx):  
  - Если текущий день недели — понедельник, среда или пятница, выведите график работы 8:00-12:00.  
  - В остальные дни недели выведите текст: Нерабочий день.  
- Для Jane Doe (yy - yy):  
  - Если текущий день недели — вторник, четверг или суббота, выведите график работы 12:00-16:00.  
  - В остальные дни недели выведите текст: Нерабочий день.
<img width="999" height="545" alt="image" src="https://github.com/user-attachments/assets/8f964558-8c9b-4891-9c2c-b9bee0f859bc" />

*(скрин сделан в понедельник)*  
Пример реализации:

```php
<?php
$day = date("N");

// John Styles
if ($day == 1 || $day == 3 || $day == 5) {
    $john_schedule = "8:00-12:00";
} else {
    $john_schedule = "Нерабочий день";
}

// Jane Doe
if ($day == 2 || $day == 4 || $day == 6) {
    $jane_schedule = "12:00-16:00";
} else {
    $jane_schedule = "Нерабочий день";
}

$employees = [
    ["id" => 1, "name" => "John Styles", "schedule" => $john_schedule],
    ["id" => 2, "name" => "Jane Doe",    "schedule" => $jane_schedule],
];
?>
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>График работы</title>
  <style>
    table {
      border-collapse: collapse;
      color: #000000;
    }

    th, td {
      border: 1px solid #000000;
      padding: 8px 16px;
      text-align: left;
    }

    th {
      font-weight: bold;
    }

  </style>
</head>
<body>
  <table>
    <thead>
      <tr>
        <th>№</th>
        <th>Фамилия Имя</th>
        <th>График работы</th>
      </tr>
    </thead>
    <tbody>
      <?php foreach ($employees as $emp): ?>
        <tr>
          <td><?= $emp['id'] ?></td>
          <td><?= htmlspecialchars($emp['name']) ?></td>
          <td class="<?= $emp['schedule'] === 'Нерабочий день' ? 'day-off' : '' ?>">
            <?= htmlspecialchars($emp['schedule']) ?>
          </td>
        </tr>
      <?php endforeach; ?>
    </tbody>
  </table>
</body>
</html>
```  

### Циклы
1. Создайте файл index.php со следующим кодом:  

```php
<?php

$a = 0;  
$b = 0;  

for ($i = 0; $i <= 5; $i++) {  
   $a += 10;  
   $b += 5;  
}  

echo "End of the loop: a = $a, b = $b";`
```  
2. Добавьте вывод промежуточных значений $a и $b на каждом шаге цикла.
```php
<?php

$a = 0;
$b = 0;

for ($i = 0; $i <= 5; $i++) {
   $a += 10;
   $b += 5;
   echo "Шаг $i: a = $a, b = $b <br>";
}

echo "End of the loop: a = $a, b = $b";
```
4. Перепишите этот цикл, используя оператор while и do-while.  
Вариант с while:
```php
<?php

$a = 0;
$b = 0;
$i = 0;

while ($i <= 5) {
   $a += 10;
   $b += 5;
   echo "Шаг $i: a = $a, b = $b <br>";
   $i++;
}

echo "End of the loop: a = $a, b = $b";
```
Вариант с do-while:
```php
<?php

$a = 0;
$b = 0;
$i = 0;

do {
   $a += 10;
   $b += 5;
   echo "Шаг $i: a = $a, b = $b <br>";
   $i++;
} while ($i <= 5);

echo "End of the loop: a = $a, b = $b";
```
<img width="1274" height="361" alt="image" src="https://github.com/user-attachments/assets/16917923-bc6b-4ca1-af2b-59975bfe97fc" />


## Контрольные вопросы

### 1. В чем разница между циклами `for`, `while` и `do-while`? В каких случаях лучше использовать каждый из них?

- **for**
  - Используется, когда заранее известно количество повторений.
  - Инициализация счётчика, условие и изменение переменной находятся в одной строке.
  - Удобен для перебора диапазона чисел или массивов.

- **while**
  - Выполняется, пока условие истинно.
  - Проверка условия происходит **до** выполнения тела цикла.
  - Используется, когда количество итераций заранее неизвестно и зависит от условия.

- **do-while**
  - Сначала выполняет тело цикла, затем проверяет условие.
  - Гарантирует минимум одно выполнение.
  - Применяется, когда код должен выполниться хотя бы один раз независимо от условия.

### 2. Как работает тернарный оператор `? :` в PHP?

Тернарный оператор — это сокращённая форма конструкции `if-else`.

**Синтаксис:**

```php
$result = (условие) ? значение_если_true : значение_если_false;
```
Пример:
```php
$age = 18;
$status = ($age >= 18) ? "Совершеннолетний" : "Несовершеннолетний";
```
Если условие истинно — возвращается первое значение, иначе второе.  

### 3. Что произойдет, если в do-while поставить условие, которое изначально ложно?

Тело цикла выполнится один раз, потому что проверка условия происходит после выполнения кода.
